export function createExperience(company, product, thumbnails, description, contributions) {
    return {
        company,
        product,
        thumbnails,
        description,
        contributions,
    }
}
